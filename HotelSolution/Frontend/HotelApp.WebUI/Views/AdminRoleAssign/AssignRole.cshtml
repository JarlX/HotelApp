@model List<HotelApp.WebUI.Models.Role.RoleAssignViewModel>

@{
    Layout = "~/Views/AdminLayout/_AdminLayout.cshtml";
}

<form method="post">
    @for (var i = 0; i < Model.Count; i++)
    {
        <div class="form-check">
            <input type="hidden" asp-for="@Model[i].RoleID"/>
            <input type="hidden" asp-for="@Model[i].RoleName"/>
            <input type="checkbox" asp-for="@Model[i].RoleExist" class="form-check-input"/>
            <label class="form-check-label mb-2" for="@Model[i].RoleExist">@Model[i].RoleName</label>
        </div>
    }
    <br/>
    <button type="submit" class="btn btn-success">Rolleri Güncelle</button>
</form>